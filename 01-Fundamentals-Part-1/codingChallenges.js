// JavaScript Fundamentals - Part 1
//
// Coding Challenge #1
//   weight in kg; height in m
/*
// const massMark = 78;
// const heightMark = 1.69;
// const massJohn = 92;
// const heightJohn = 1.95;
const massMark = 95;
const heightMark = 1.88;
const massJohn = 85;
const heightJohn = 1.76;

const bmiMark = massMark / heightMark ** 2;
const bmiJohn = massJohn / heightJohn ** 2;

const markHigherBmi = bmiMark > bmiJohn;

// Coding Challenge #2
if (bmiMark > bmiJohn) {
  console.log(`Mark's BMI (${bmiMark}) is higher than John's (${bmiJohn})!`);
} else {
  console.log(`John's BMI (${bmiJohn}) is higher than Mark's (${bmiMark})!`);
}


// Coding Challenge #3
// const dolphinsAvgScore = 96 + 108 + 89;
// const koalasAvgScore = 88 + 91 + 110;
// Bonus #1
// const dolphinsAvgScore = (97 + 112 + 101) / 3;
// const koalasAvgScore = (109 + 95 + 123) / 3;
// Bonus #2
const dolphinsAvgScore = (97 + 112 + 101) / 3;
const koalasAvgScore = (109 + 95 + 106) / 3;

console.log(dolphinsAvgScore, koalasAvgScore);

// The basic challenge
if (dolphinsAvgScore > koalasAvgScore && dolphinsAvgScore >= 100) {
  console.log(`Dolphins win the trophy 🏆`);
} else if (koalasAvgScore > dolphinsAvgScore && koalasAvgScore >= 100) {
  console.log(`Koalas win the trophy 🏆`);
} else if (
  dolphinsAvgScore === koalasAvgScore &&
  dolphinsAvgScore >= 100 &&
  koalasAvgScore >= 100
) {
  console.log(`Both win the trophy 🏆`);
} else {
  console.log("No one wins the trophy!");
}


// Coding Challenge #4
const bill = 275;
const tip = bill >= 50 && bill <= 300 ? bill * 0.15 : bill * 0.2;
console.log(
  `The bill value was ${bill}, the tip value was ${tip}, and the total value is ${
    bill + tip
  }`
);
*/
